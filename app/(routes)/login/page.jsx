import LoginForm from "@/app/_components/LoginForm";
import React from "react";
export const metadata = {
  title: "login page",
};

const page = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default page;
